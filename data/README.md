# Data Folder

This folder is used locally for datasets and generated images.

The full dataset is not committed to GitHub because it contains many image files.

Expected local structure:

data/
  raw/
  standardized/
  roboflow/
    azota-layout-v1/
      train/
      valid/
      test/
      data.yaml
  samples/